---
name: shb__building__architecture
description: transform your LLM call inventory into a clean agent architecture with clear boundaries, control loops, and handoff contracts
disable-model-invocation: true
---

Run the **Agent Architecture Design** workflow from the Sherpa-B MCP server.

Call `activity/get-workflow` with `activity_id: "architecture"` to get the workflow structure, then follow `workflow_execution_instructions` to execute it state by state.
