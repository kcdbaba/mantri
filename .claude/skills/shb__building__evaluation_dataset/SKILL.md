---
name: shb__building__evaluation_dataset
description: design systematic evaluation for AI workflows through risk-driven test case creation
disable-model-invocation: true
---

Run the **Evaluation Dataset Creation Workflow** workflow from the Sherpa-B MCP server.

Call `activity/get-workflow` with `activity_id: "evaluation_dataset"` to get the workflow structure, then follow `workflow_execution_instructions` to execute it state by state.
