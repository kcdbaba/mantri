---
name: shb__project-management__create-issues
description: Iteratively create GitHub issues with contextual code review
disable-model-invocation: true
---

Run the **Create Issues** workflow from the Sherpa-B MCP server.

Call `activity/get-workflow` with `activity_id: "create-issues"` to get the workflow structure, then follow `workflow_execution_instructions` to execute it state by state.
