---
name: shb__building__orientation
description: First-time onboarding for bootcamp participants - sets up workspace and gets to know them
disable-model-invocation: true
---

Run the **Orientation Workflow** workflow from the Sherpa-B MCP server.

Call `activity/get-workflow` with `activity_id: "orientation"` to get the workflow structure, then follow `workflow_execution_instructions` to execute it state by state.
