---
name: shb__building__evaluation_at_scale
description: transform your agent architecture into a diagnostic evaluation system by validating context inputs structurally, evaluating outputs semantically, and tracing failures back to context management issues
disable-model-invocation: true
---

Run the **Evaluation at Scale** workflow from the Sherpa-B MCP server.

Call `activity/get-workflow` with `activity_id: "evaluation_at_scale"` to get the workflow structure, then follow `workflow_execution_instructions` to execute it state by state.
